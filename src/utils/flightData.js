// Import static flight data
import staticFlightData, { airlines, airports, cabinClasses } from '../data/staticFlightData.js';

// Directly export static flight data
export const flightData = staticFlightData;

// Export other related data
export { airlines, airports, cabinClasses }; 